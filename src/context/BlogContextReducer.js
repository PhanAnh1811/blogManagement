
/**
 * 1. Create a reducer
 */

const intialState =[];

const BlogReducer = (state,action) => {
    switch(action.type){
        case 'GET_DATA':
            return state;
        default:
            return state;
    }
}

const getList = (dispatch) => {
    return async () => {
        
    }
}